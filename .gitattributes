# 本工程的文件字节必须逐字保留，因此全局关闭行尾转换。
#
#   *.ps1  是 UTF-8 with BOM。没有 BOM 时 PowerShell 5.1 会按 ANSI 解码，
#          中文变乱码，并且会吞掉与之相邻的 $（这个坑真的踩过）。
#   apk/res、apk/assets  下有二进制资源。
#
# 任何 CRLF/LF 归一化都会改动这些字节，所以这里显式声明不做文本处理。
* -text
