root = true

[*]
charset = utf-8
insert_final_newline = true
trim_trailing_whitespace = true

# Windows PowerShell 5.1 会把没有 BOM 的 .ps1 按 ANSI 解码：中文变乱码，
# 且乱码会吞掉字符串的结束引号。所以 .ps1 一律 UTF-8 with BOM。
[*.ps1]
charset = utf-8-bom

# Markdown 里行尾两个空格是硬换行，不能裁掉。
[*.md]
trim_trailing_whitespace = false
